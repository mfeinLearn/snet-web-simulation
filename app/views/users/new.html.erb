<h1>Sign up here!</h1>
<%= form_for(@user) do |f|%>
<%= render partial: 'layouts/errors', locals: {object: @user} %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :password %>
<%= f.password_field :password %>

<%= f.submit "Sign In" %>


<% end  %>
